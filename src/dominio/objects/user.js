export default class User {

  constructor(id, name, age, email, password, balance) {

    this.id = id;
    this.name = name;
    this.age = age;
    this.email = email;
    this.password = password;
    this.balance = balance;
  }
}